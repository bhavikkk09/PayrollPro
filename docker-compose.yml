version: '3.8'

services:
  payrollpro-hrms:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: payrollpro_hrms_app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped
